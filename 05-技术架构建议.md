# 05 技术架构建议

> 状态：v0.1 技术基线已冻结，供《1-1 Agent的记忆有边界》纵向样板直接采用。
>
> 冻结范围包括技术栈、内容与本地存储方案、测试组合和静态站目标。构建物未来可由 html-go-live 发布；当前没有发布授权，本文不构成发布指令，也不得执行发布。

## 1. 推荐架构

- 前端：React + Vite + TypeScript
- 样式：CSS Variables + Tailwind CSS（视觉Token统一管理）
- 动效：Framer Motion，仅用于页面切换、节点状态和反馈
- 内容：课程JSON/TypeScript配置驱动，内容与组件分离
- 状态：轻量状态容器 + 统一Storage Adapter
- 测试：Vitest + Testing Library + Playwright
- 静态站目标：构建物未来可由 html-go-live 发布；当前没有发布授权

## 2. 功能分层

1. **内容层**：课程、对话节点、实验、题目、解析、来源依据
2. **课程引擎**：节点推进、分支判断、完成条件、错题与收藏
3. **界面层**：学习地图、课程页、实验组件、复习页、主题系统
4. **存储层**：localStorage适配、Schema迁移、导入/导出
5. **AI增强层**：课后自由提问；失败时不影响预设课程

## 3. 建议目录

```text
src/
  app/                 # 路由、主题、全局状态
  features/
    learning-map/      # 学习地图
    lesson-player/     # 对话课程引擎
    experiments/       # 互动实验组件
    quiz/              # 测验、解析、错题
    review/            # 收藏与复习
    ask-hongshu/       # 课后AI提问
  content/
    curriculum.ts      # 模块与课程索引
    lessons/           # 单课脚本与题库
  shared/
    ui/                # 基础视觉组件
    storage/           # Storage Adapter与迁移
    types/             # 领域类型
```

## 4. 课程数据契约

```ts
interface Lesson {
  id: string;
  moduleId: string;
  title: string;
  durationMinutes: number;
  objectives: string[];
  sourceRefs: SourceRef[];
  steps: LessonStep[];
  quiz: QuizQuestion[];
  relatedLessonIds: string[];
}

interface LessonStep {
  id: string;
  type: 'scene' | 'dialogue' | 'experiment' | 'quiz' | 'summary' | 'free-question';
  speaker?: 'hongshu' | 'learner' | 'system';
  content: string;
  options?: StepOption[];
  experimentId?: string;
}
```

## 5. 本地进度模型

```json
{
  "schemaVersion": 1,
  "theme": "light",
  "currentLessonId": "1-1",
  "currentStepId": "dialogue-03",
  "completedLessonIds": ["0-1", "0-2"],
  "answers": {},
  "experimentStates": {},
  "wrongQuestionIds": [],
  "favoriteConceptIds": [],
  "baselineAssessment": {},
  "lastVisitedAt": "ISO-8601"
}
```

必须处理：无痕模式写入失败、JSON损坏、Schema升级、多标签页冲突、存储不足、导入数据校验。

## 6. AI自由问答边界

- **真实模型服务默认关闭**：样板不得接入线上模型，也不得保存前端密钥。
- **预置FAQ降级为样板必做功能**：在服务关闭、失败、超时或不可用时，FAQ必须可用且不阻断预设课程。
- 如未来启用调用，调用仅能放服务端，前端不保存密钥。
- 输入长度、频率和超时受控。
- 只检索当前课和关联课程的审核知识。
- 不确定时明确说明，超范围时推荐课程。
- 页面明确提示不得提交公司机密和个人敏感信息。

真实模型服务、知识来源和调用预算均为后续单独授权事项；未获授权不阻断既定预设课程。

## 7. 非功能要求

- 断点恢复成功率：100%
- 标准课程离线/无AI服务仍可完成
- 桌面端优先，移动端所有流程无需拖拽也可完成
- 关键交互满足WCAG 2.1 AA：键盘可达、焦点可见、对比度合格
- 首屏目标：常规内网环境下2.5秒内可交互
