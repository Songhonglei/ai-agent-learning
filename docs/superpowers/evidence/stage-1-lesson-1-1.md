# 阶段1验收证据：1-1《Agent的记忆有边界》

> 验收日期：2026-08-05；验收结论：阶段1纵向样板通过，下一步进入阶段2课程引擎定型。

## 1. 来源输入

- `reference/source-audit/lesson-1-1-source-pack.md`：限定本课事实链、互动与测验依据、`figure-2-1` 语义重绘及缓存扩展边界。
- `reference/project-baseline-v0.2.md`：限定阶段1只恢复本课进度，不实现全局 Schema 迁移或导入/导出。
- `03-视觉设计规范.md` 与 `visuals/hongshu-avatar.svg`：冻结 Light/Dark Token、布局和红叔角色资产。
- `02-课程大纲.md`：限定 5 个 Module、12 课、6 个学习地图节点及 1-1 在课程结构中的位置。

本课页面只引用已复核的 `figure-2-1`、`page-035`、`page-052`，并明确将 `page-052` 标为生产级缓存扩展边界，不进入课内判定。

## 2. 已实现交互

1. 新用户从学习地图直接进入 1-1；其他 11 课只展示介绍与推荐顺序。
2. 按六个预设步骤完成情境、对话、上下文构建器、三道情境测验、小结与 FAQ。
3. 上下文构建器要求选择代码/报错、流程/分支约束、测试/运行环境三项必要背景；无关路线图不能替代任一必要项。
4. 三道测验均使用本地确定性判定，并展示即时判断、深度解析及原书依据；答错不阻断课程。
5. 来源入口展示 PDF 34、35、52 的来源 ID、结论与边界，并包含 `figure-2-1` 的可访问 HTML/CSS 语义重绘和完整文字说明。
6. FAQ 只展开审核内容；自由提问输入和发送按钮保持禁用，不发出 Fetch/XHR 请求。
7. 本课当前步骤、上下文选择、答案和主题写入 localStorage；刷新后恢复，存储异常不阻断课程。

## 3. 自动化测试与结果

端到端测试由 Playwright 启动 `127.0.0.1:4173` 本地 Vite 服务；场景监听全部页面请求并断言没有外部主机请求，FAQ 展开期间 Fetch/XHR 请求数为 0。

| 命令 | 结果 |
|---|---|
| `npm run test:e2e -- e2e/lesson-1-1.spec.ts`（初始红灯） | 失败：旧配置只扫描 `tests/e2e`，新增场景未被发现；证明端到端入口尚未接通。 |
| `npm run test:e2e -- e2e/lesson-1-1.spec.ts` | 通过：4/4；覆盖 1440px 与 375px 完整流程、四宽度响应式、减弱动态效果。 |
| `npm run test:run` | 通过：13 个测试文件、50 个测试。 |
| `npm run build` | 通过：TypeScript 构建与 Vite 生产构建成功。 |
| `npm run test:e2e` | 通过：4/4。 |
| `git diff --check` | 通过：无空白错误。 |

首次运行 Playwright 时本机缺少与 1.58.2 对应的 Chromium 1234；仅补装本地 Chromium 与 Headless Shell 后完成验收，没有把浏览器产物写入仓库。

## 4. 响应式与移动端验收

- 375px：从地图进入课程，选择三项必要上下文、完成三题、查看来源、展开 FAQ、刷新恢复，完整链路通过；关键动作未隐藏，页面无横向溢出。
- 768px、1024px、1440px：返回地图、下一步、查看来源和主题切换均可见，页面无横向溢出。
- 课程目录在窄屏改为可横向滚动的步骤条，内容区、上下文卡片、来源卡片与 FAQ 改为单列；关键流程不依赖拖拽。
- `prefers-reduced-motion: reduce` 下，非必要动画与过渡被压缩至 `0.01ms`，滚动行为改为即时；流程动作仍可完成。

## 5. 明确未做事项

- 未发布、未部署、未推送，也未执行任何远程写入。
- 未接入真实模型、AI 服务、前端密钥或网络教学依赖；FAQ 不生成临场回答。
- 未提前实现阶段2能力：通用课程 Schema/协议、Schema 迁移、学习进度导入/导出、错题、收藏、复习和前测。
- 未批量生产剩余 11 课，也未执行阶段4全量联调与发布工作。
