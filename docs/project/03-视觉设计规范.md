# 03 视觉设计规范

> **文档状态：** v0.1 样板视觉基线已冻结。
> **面向对象：** UI设计师、前端工程师

> **冻结范围：** 本文色彩、字体、布局和 `visuals/hongshu-avatar.svg` 是《1-1 Agent的记忆有边界》纵向样板的直接实现基线。不得因样板开发重新选择视觉方向；变更须另行决策记录。

---

## 1. 设计原则

- **面向职场人：** 专业、清晰、有适度科技感
- **不儿童化：** 避免过于可爱的元素
- **双主题：** Light/Dark 一键切换
- **信息清晰：** 内容优先，装饰克制

---

## 2. 色彩系统

### 2.1 Light主题

| 色值 | 用途 | CSS变量 |
|---|---|---|
| `#f6f7fb` | 页面背景 | `--bg` |
| `#ffffff` | 卡片/面板背景 | `--panel` |
| `#172033` | 主文字 | `--text` |
| `#6f7890` | 次要文字/辅助 | `--muted` |
| `#e6e9f0` | 分割线/边框 | `--line` |
| `#ea4b52` | 主品牌色（红叔红） | `--primary` |
| `#5365ed` | 强调色（靛蓝） | `--indigo` |
| `#f0f2ff` | 柔和背景/标签 | `--soft` |

### 2.2 Dark主题

| 色值 | 用途 | CSS变量 |
|---|---|---|
| `#0d1324` | 页面背景 | `--bg` |
| `#151d31` | 卡片/面板背景 | `--panel` |
| `#f1f4ff` | 主文字 | `--text` |
| `#9aa5bf` | 次要文字/辅助 | `--muted` |
| `#293450` | 分割线/边框 | `--line` |
| `#ea4b52` | 主品牌色（红叔红） | `--primary` |
| `#5365ed` | 强调色（靛蓝） | `--indigo` |
| `#1d2742` | 柔和背景/标签 | `--soft` |

### 2.3 阴影

```css
--shadow: 0 18px 55px rgba(28,38,72,.09);  /* Light */
--shadow: 0 18px 55px rgba(0,0,0,.25);      /* Dark */
```

---

## 3. 字体规范

| 层级 | 字号 | 字重 | 用途 |
|---|---|---|---|
| H1 | 42px | 800 | 首页大标题 |
| H2 | 31px | 700 | 页面标题 |
| 正文 | 16px | 400 | 段落文字 |
| 辅助文字 | 13px | 600 | 标签、元信息 |
| 小字 | 11-12px | 400-600 | 节点说明、提示 |

**字体栈：**
```css
font-family: Inter, "PingFang SC", "Microsoft YaHei", sans-serif;
```

---

## 4. 布局规范

### 4.1 全局容器

```css
.shell {
  max-width: 1200px;
  margin: 0 auto;
  padding: 22px;
}
```

### 4.2 首页布局

```
+------------------------------------------+
| 顶部导航栏 (高度64px, 圆角18px)            |
+------------------------------------------+
|                                          |
|  Hero区域 (grid: 1.35fr .65fr)            |
|  左侧：标题+描述+CTA按钮                     |
|  右侧：红叔导师卡片 (290px宽)               |
|                                          |
+------------------------------------------+
|                                          |
|  学习地图区域 (12列网格)                     |
|  6个学习节点沿路径排列                      |
|                                          |
+------------------------------------------+
```

### 4.3 课程页布局

```
+-----------+----------------------+----------+
| 侧边栏    |  课程主内容区         | 红叔面板  |
| (240px)   |  (自适应)             | (310px)  |
|           |                      |          |
| 模块列表  |  对话内容              | 红叔头像  |
|           |  聊天气泡             | 状态信息  |
|           |  选项按钮             |          |
|           |                      |          |
+-----------+----------------------+----------+
```

---

## 5. 组件规范

### 5.1 导航栏

```css
.top {
  height: 64px;
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 18px;
  display: flex;
  align-items: center;
  padding: 0 20px;
  box-shadow: var(--shadow);
}
```

### 5.2 按钮

**主要按钮（CTA）：**
```css
.continue {
  border: 0;
  border-radius: 13px;
  background: var(--primary);
  color: #fff;
  font-weight: 750;
  padding: 13px 20px;
}
```

**标签按钮：**
```css
.nav button, .tabs button {
  border: 0;
  background: transparent;
  color: var(--muted);
  padding: 9px 13px;
  border-radius: 10px;
  font-weight: 600;
  cursor: pointer;
}
.nav .active, .tabs .active {
  background: var(--soft);
  color: var(--indigo);
}
```

### 5.3 导师卡片

```css
.mentor-card {
  align-self: center;
  justify-self: end;
  width: 290px;
  padding: 17px;
  background: rgba(255,255,255,.65);
  border: 1px solid rgba(255,255,255,.6);
  backdrop-filter: blur(18px);
  border-radius: 20px;
  display: flex;
  gap: 14px;
  align-items: center;
  box-shadow: var(--shadow);
}
```

### 5.4 学习节点

```css
.dot {
  width: 68px;
  height: 68px;
  border-radius: 22px;
  margin: auto;
  display: grid;
  place-items: center;
  font-size: 22px;
  background: var(--panel);
  border: 2px solid var(--line);
  box-shadow: 0 8px 22px rgba(28,38,72,.08);
}
.done .dot { background: var(--primary); color: #fff; border-color: var(--primary); }
.current .dot { background: var(--indigo); color: #fff; border-color: var(--indigo); }
```

### 5.5 对话气泡

```css
.bubble {
  max-width: 650px;
  padding: 15px 18px;
  border-radius: 6px 18px 18px 18px;
  background: var(--soft);
  line-height: 1.7;
  font-size: 14px;
}
.user .bubble {
  border-radius: 18px 6px 18px 18px;
  background: var(--indigo);
  color: #fff;
}
```

---

## 6. 响应式断点

| 断点 | 调整 |
|---|---|
| < 768px | 单列布局，侧边栏收起，导师卡片下移 |
| 768-1024px | 双列布局，红叔面板收起 |
| > 1024px | 三列布局，完整展示 |

---

## 7. 视觉稿文件

| 文件 | 说明 |
|---|---|
| `visuals/01-learning-map-demo.html` | 学习地图首页Demo |
| `visuals/02-dialogue-lesson-demo.html` | 对话学习页Demo |
| `visuals/01-learning-map.png` | 学习地图首页截图 |
| `visuals/02-dialogue-lesson.png` | 对话学习页截图 |
| `visuals/hongshu-avatar.svg` | 红叔头像SVG |

---

## 8. 阶段1验收项（不阻塞样板开发）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 移动端页面视觉 | 阶段1验收项；不阻塞样板开发 |
| 2 | 加载、空状态和错误状态 | 阶段1验收项；不阻塞样板开发 |
