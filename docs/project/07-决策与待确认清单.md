# 07 决策与待确认清单

## 已确认

1. 目标用户：技术背景不高的普通白领。
2. 产品定位：约4小时完成Agent原理、边界与办公应用学习。
3. 内容比例：40%原理 + 60%办公应用。
4. v0.1：知识地图 + 对话导师，12课，Light/Dark，本地进度，预设课程 + 课后自由提问，3题基线前测。
5. v0.2：杂志卡片式自由浏览、路径推荐、用户分层、线上数据同步。
6. 标准答案必须预先审核；答错不惩罚、不阻断。
7. 导师为“红叔”，成熟、直接、简洁、适度幽默，不冒充真人或作者。
8. 学习地图是首页核心视觉，对话页是主学习界面。
9. 课程学习地图固定为6个节点，覆盖既有12课：认识 Agent（0-1、0-2）、看懂上下文（1-1、1-2、1-3）、记忆与知识（2-1、2-2、2-3）、工具与行动（3-1、3-2）、评估 Agent（4-1）、多 Agent（4-2）。
10. 课程边界冻结在既有12课内：3-1纳入权限、人工确认和不可逆护栏；3-2纳入失败恢复和能力边界；4-2纳入多Agent有效的限定条件。
11. v0.1采用软锁定：课程允许直接进入；前置课未完成时仅提示推荐顺序，不阻断学习。
12. 前测与后测各3题；后测与前测等价，且不直接复用题干。

## v0.1 冻结基线

1. 先做《1-1 Agent的记忆有边界》纵向样板，再批量扩展。
2. 课程采用软锁定：推荐顺序但允许直接进入课程，不阻断学习。
3. 首期使用localStorage + Schema版本 + 导入/导出。
4. 视觉样板采用 `03-视觉设计规范.md` 的色彩、字体、布局和 `visuals/hongshu-avatar.svg`；移动端、加载、空和错误状态为阶段1验收项，不阻塞样板开发。
5. 技术栈冻结为 React + Vite + TypeScript、CSS Variables + Tailwind、轻量状态容器、Storage Adapter、Vitest + Testing Library + Playwright。
6. 构建物的未来静态发布目标为 html-go-live；当前没有发布授权，不得发布。
7. 真实模型服务默认关闭，样板必须提供预置FAQ降级，绝不影响核心课程；前端不得保存密钥或接入线上模型。
8. 来源完整性、断点恢复、题目人工审核和关键无障碍项为硬门槛；开始率、完成率、前后测提升和端间差异为观测目标。
9. 桌面端优先，同时保证移动端流程可完成。

## 阶段0完成记录（2026-08-05）

- 已形成唯一执行摘要：`reference/project-baseline-v0.2.md`；阶段1直接按该基线启动《1-1 Agent的记忆有边界》纵向样板。
- 阶段A来源审计已通过；1-1来源包、可重绘语义核心图、视觉Token与课程数据契约均为已具备输入。
- v0.1范围内不存在阻塞样板启动的待确认事项；“移动端、加载、空和错误状态”转为阶段1验收项，不改变已冻结视觉方向。
- 本阶段未发布、未远程写入、未接入或调用真实模型。

## 仍须单独授权

1. 任何 html-go-live 发布或其他远程写入。
2. 真实模型服务的启用，以及其服务商、知识来源和调用预算。
